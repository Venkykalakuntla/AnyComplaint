<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale-1.0" />
    <title>Complaint Details</title>
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css"
      rel="stylesheet"
    />
    <link
      rel="stylesheet"
      href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css"
    />
    <link rel="stylesheet" href="/styles/complaint-detail.css" />
  </head>
  <body>
    <%- include('partials/navbar') %>

    <div class="container my-5">
      <header class="d-flex justify-content-between align-items-center mb-4">
        <h1>Complaint Details</h1>
        <a href="/dashboard" class="btn btn-outline-secondary"
          >&larr; Back to Dashboard</a
        >
      </header>

      <div class="row g-4">
        <div class="col-lg-4">
          <aside class="glass-card meta-data sticky-sidebar">
            <h3>Information</h3>
            <p><strong>Category:</strong> <%= complaint.category %></p>
            <p><strong>Status:</strong> <%= complaint.status %></p>
            <p>
              <strong>Created On:</strong> <%= new
              Date(complaint.createdAt).toLocaleString('en-IN') %>
            </p>
            <p>
              <strong>Recommended Portal:</strong>
              <a
                href="<%= complaint.portal %>"
                target="_blank"
                rel="noopener noreferrer"
                ><%= complaint.portal %></a
              >
            </p>
            <div class="d-grid">
              <button
                class="btn btn-gradient"
                onclick="copyTextFromElement('complaintText', 'Complaint draft copied to clipboard!')"
              >
                <i class="bi bi-clipboard-check-fill me-2"></i>Copy Draft Text
              </button>
            </div>
          </aside>
        </div>

        <div class="col-lg-8">
          <main class="glass-card">
            <h3>Complaint Draft</h3>
            <pre class="complaint-draft-text" id="complaintText">
<%= complaint.complaintDraft %></pre
            >
          </main>
        </div>
      </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
    <script src="/js/script.js"></script>
  </body>
</html>
