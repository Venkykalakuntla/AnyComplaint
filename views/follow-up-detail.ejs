<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Follow-Up Draft</title>
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css"
      rel="stylesheet"
    />
    <link
      rel="stylesheet"
      href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css"
    />
    <style>
      body {
        background-color: #121212;
        color: #ffffff;
        font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto,
          Helvetica, Arial, sans-serif;
      }
      .glass-card {
        background: rgba(255, 255, 255, 0.1);
        backdrop-filter: blur(10px);
        border: 1px solid rgba(255, 255, 255, 0.2);
        border-radius: 16px;
        padding: 2rem;
        box-shadow: 0 8px 32px 0 rgba(0, 0, 0, 0.3);
      }
      h1 {
        font-size: 2.5rem;
        font-weight: 700;
        background: linear-gradient(90deg, #2ba0e9, #11f9f9);
        -webkit-background-clip: text;
        background-clip: text;
        color: transparent;
      }
      h3 {
        color: rgba(255, 255, 255, 0.9);
      }
      pre {
        white-space: pre-wrap;
        word-wrap: break-word;
        font-family: "Georgia", serif;
        font-size: 1.1rem;
        line-height: 1.8;
        color: rgba(255, 255, 255, 0.8);
      }
      .btn-gradient {
        background: linear-gradient(90deg, #2ba0e9, #11f9f9);
        color: #ffffff;
        font-weight: 600;
        border: none;
        transition: all 0.3s ease;
      }
      .btn-gradient:hover {
        transform: translateY(-2px);
        box-shadow: 0 4px 20px rgba(0, 124, 240, 0.3);
        color: white;
      }
    </style>
  </head>
  <body>
       <%- include('partials/navbar') %>
       
    <div class="container my-5">
      <header class="d-flex justify-content-between align-items-center mb-4">
        <h1>AI-Generated Follow-Up</h1>
        <a href="/dashboard" class="btn btn-outline-secondary"
          >&larr; Back to Dashboard</a
        >
      </header>

      <div class="row g-4">
        <div class="col-lg-7">
          <main class="glass-card">
            <div class="d-flex justify-content-between align-items-center mb-3">
              <h3>Your Follow-Up Letter</h3>
              <button class="btn btn-gradient btn-sm" onclick="copyDraft()">
                <i class="bi bi-clipboard-check-fill me-2"></i>Copy Text
              </button>
            </div>
            <pre id="followUpText"><%= followUpDraft %></pre>
          </main>
        </div>

        <div class="col-lg-5">
          <aside class="glass-card" style="opacity: 0.7">
            <h3 class="h5">Original Complaint (for reference)</h3>
            <pre style="font-size: 0.9rem; line-height: 1.6">
<%= originalComplaint.complaintDraft %></pre
            >
          </aside>
        </div>
      </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
     <script src="/js/script.js"></script>
  </body>
</html>
