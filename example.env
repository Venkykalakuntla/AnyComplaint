# --- Google Gemini API Configuration ---
# Get your API key from Google AI Studio: https://aistudio.google.com/app/apikey
GEMINI_API_KEY=YOUR_GEMINI_API_KEY_HERE

# --- Server Configuration ---
# The port your Node.js application will run on (e.g., 8080 or 3000)
PORT=8080

# --- MongoDB Database Configuration ---
# Your MongoDB connection string (e.g., from MongoDB Atlas)
MONGO_URI=YOUR_MONGODB_CONNECTION_STRING

# --- Session Management ---
# A long, random, secret string used to sign the session ID cookie
SESSION_SECRET=REPLACE_WITH_A_STRONG_RANDOM_SECRET

# --- Email Configuration (for Nodemailer with Gmail) ---
# Gmail SMTP server address
EMAIL_HOST=smtp.gmail.com
# Gmail SMTP port for SSL/TLS
EMAIL_PORT=465
# Your full Gmail address (the account that will send emails)
EMAIL_USER=YOUR_GMAIL_ADDRESS@gmail.com
# Your 16-digit Google App Password (generate in Google Account Security settings)
EMAIL_PASS=YOUR_16_DIGIT_GMAIL_APP_PASSWORD_NO_SPACES

# --- Optional: Node Environment ---
# Set to 'development' for local work, 'production' when deployed
# NODE_ENV=development